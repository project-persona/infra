{
  "name": "@persona/infra",
  "version": "0.1.0",
  "description": "in-house infrastructure and tooling for the Persona project",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start-broker": "npm start --prefix ./service-broker",
    "start-gateway": "npm start --prefix ./api-gateway",
    "lint": "standard",
    "test": "npm run lint && echo \"Warning: no test specified\"",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:project-persona/infra.git"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@persona/api-gateway": "file:api-gateway",
    "@persona/service-broker": "file:service-broker",
    "@persona/web-sdk": "file:web-sdk"
  },
  "devDependencies": {
    "husky": "^7.0.1",
    "standard": "^16.0.3"
  }
}
